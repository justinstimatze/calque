# Copy to .env (which is gitignored) and fill in your key.
# Used by `calque propose-deep --judge` — the LLM equivalence oracle.
ANTHROPIC_API_KEY=sk-ant-...

# Optional — cheaper/faster bulk judging (default is claude-opus-4-8):
# CALQUE_JUDGE_MODEL=claude-haiku-4-5
